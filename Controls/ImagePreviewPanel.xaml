<UserControl x:Class="KaneCode.Controls.ImagePreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource EditorBackground}">
    <Grid>
        <!-- Viewport with pan (drag) and zoom (mouse wheel) -->
        <Border x:Name="Viewport"
                ClipToBounds="True"
                Background="#FF1C1C1C"
                MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                MouseMove="Viewport_MouseMove"
                MouseWheel="Viewport_MouseWheel"
                Focusable="True">
            <Grid>
                <Grid.Background>
                    <!-- Checkerboard so transparency in PNG/SVG renders is visible -->
                    <DrawingBrush TileMode="Tile"
                                  Viewport="0,0,16,16"
                                  ViewportUnits="Absolute">
                        <DrawingBrush.Drawing>
                            <DrawingGroup>
                                <GeometryDrawing Brush="#FF3A3A3A">
                                    <GeometryDrawing.Geometry>
                                        <RectangleGeometry Rect="0,0,16,16" />
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                                <GeometryDrawing Brush="#FF262626">
                                    <GeometryDrawing.Geometry>
                                        <GeometryGroup>
                                            <RectangleGeometry Rect="0,0,8,8" />
                                            <RectangleGeometry Rect="8,8,8,8" />
                                        </GeometryGroup>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingGroup>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Grid.Background>

                <Image x:Name="PreviewImage"
                       Stretch="None"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ScaleXform" />
                            <TranslateTransform x:Name="TranslateXform" />
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>

                <TextBlock x:Name="ErrorText"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"
                           MaxWidth="480"
                           Foreground="#FFFF8A8A"
                           FontSize="13"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="24" />
            </Grid>
        </Border>

        <!-- Overlay: zoom indicator, fit / 1:1 buttons, interaction hint -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,0,10,8"
                    Opacity="0.85">
            <Border Background="#CC222222"
                    CornerRadius="3"
                    Padding="6,2"
                    VerticalAlignment="Center">
                <TextBlock x:Name="ZoomText"
                           Text="100%"
                           Foreground="#FFFFFF"
                           FontSize="11" />
            </Border>
            <Button x:Name="FitButton"
                    Content="Fit"
                    Width="48"
                    Height="20"
                    FontSize="11"
                    Margin="6,0,0,0"
                    Padding="4,0"
                    Cursor="Hand"
                    Foreground="{DynamicResource StatusBarForeground}"
                    Background="{DynamicResource ControlBackground}"
                    BorderBrush="{DynamicResource ControlBorder}"
                    BorderThickness="1"
                    Click="FitButton_Click" />
            <Button x:Name="ActualSizeButton"
                    Content="1:1"
                    Width="48"
                    Height="20"
                    FontSize="11"
                    Margin="6,0,0,0"
                    Padding="4,0"
                    Cursor="Hand"
                    Foreground="{DynamicResource StatusBarForeground}"
                    Background="{DynamicResource ControlBackground}"
                    BorderBrush="{DynamicResource ControlBorder}"
                    BorderThickness="1"
                    Click="ActualSizeButton_Click" />
            <Border Background="#CC222222"
                    CornerRadius="3"
                    Padding="6,2"
                    Margin="6,0,0,0"
                    VerticalAlignment="Center">
                <TextBlock Text="Scroll to zoom · Drag to pan · Double-click to fit"
                           Foreground="#FFFFFF"
                           FontSize="11" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
