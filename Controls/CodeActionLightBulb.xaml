<UserControl x:Class="KaneCode.Controls.CodeActionLightBulb"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:KaneCode.Models"
             Visibility="Collapsed">
    <Popup x:Name="ActionPopup"
           Placement="Relative"
           StaysOpen="False"
           AllowsTransparency="True"
           Closed="ActionPopup_Closed">
        <Border Background="{DynamicResource CompletionBackground}"
                BorderBrush="{DynamicResource CompletionBorder}"
                BorderThickness="1"
                CornerRadius="2"
                MaxHeight="300"
                MinWidth="200"
                MaxWidth="500">
            <DockPanel>
                <!-- Header -->
                <Border DockPanel.Dock="Top"
                        Background="{DynamicResource CompletionBackground}"
                        Padding="8,4">
                    <TextBlock Text="Code Actions"
                               FontWeight="SemiBold"
                               FontSize="11"
                               Foreground="{DynamicResource CompletionForeground}" />
                </Border>

                <!-- Actions list -->
                <ListBox x:Name="ActionsList"
                         Background="{DynamicResource CompletionBackground}"
                         Foreground="{DynamicResource CompletionForeground}"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         MouseDoubleClick="ActionsList_MouseDoubleClick"
                         KeyDown="ActionsList_KeyDown">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,4" />
                            <Setter Property="Foreground" Value="{DynamicResource CompletionForeground}" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd"
                                                Background="Transparent"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background"
                                                        Value="{DynamicResource CompletionSelectedBackground}" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background"
                                                        Value="{DynamicResource CompletionSelectedBackground}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:CodeActionItem}">
                            <TextBlock Text="{Binding Title}"
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="12" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>
    </Popup>
</UserControl>
