<UserControl x:Class="KaneCode.Controls.AgentOrchestratorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KaneCode.Controls">
    <DockPanel Background="{DynamicResource PanelBackground}">
        <Border DockPanel.Dock="Top" Padding="8,6" Background="{DynamicResource AiChatHeaderBackground}" BorderBrush="{DynamicResource AiChatBorder}" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="Agent Orchestrator" FontWeight="SemiBold" Foreground="{DynamicResource PanelForeground}" />
                <TextBlock x:Name="SummaryText" Margin="0,3,0,0" FontSize="10" Foreground="{DynamicResource AiChatSecondaryForeground}" />
            </StackPanel>
        </Border>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="4">
            <TreeView x:Name="AgentTree" Background="Transparent" BorderThickness="0" SelectedItemChanged="AgentTree_SelectedItemChanged">
                <TreeView.Resources>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Foreground" Value="{DynamicResource PanelForeground}" />
                        <Setter Property="Padding" Value="3,4" />
                        <Setter Property="Margin" Value="0,1" />
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource TreeViewItemMouseOverBackground}" /></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource TreeViewItemSelectedBackground}" /><Setter Property="Foreground" Value="{DynamicResource TreeViewItemSelectedForeground}" /></Trigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.Resources>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Glyph}" Margin="0,0,6,0" />
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Details}" FontSize="10" Foreground="{DynamicResource AiChatSecondaryForeground}" />
                            </StackPanel>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </ScrollViewer>
    </DockPanel>
</UserControl>
